export default function DatePreview({ field }) {
  return (

        <button
          className="px-6 py-2 rounded-full bg-[#F8EDEF] text-[#C64B5F] font-medium text-[14px] shadow-sm"
          type="button"
          disabled
        >
          Select Date
        </button>

  );
}
